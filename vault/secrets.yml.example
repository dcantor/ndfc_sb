---
# SECRETS — encrypt before any real use:   ansible-vault encrypt vault/secrets.yml
# This plaintext starter exists only so the project runs end-to-end in a lab.

# NDFC / Nexus Dashboard controller login.
ndfc_username: "admin"
ndfc_password: "CHANGE_ME_ndfc"

# Per-switch device credentials NDFC uses to discover/manage the cores.
device_username: "admin"
device_password: "CHANGE_ME_device"
